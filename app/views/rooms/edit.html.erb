<section>
    <h3> Edit room details </h3>
    
    <div>
        <%= simple_form_for edit_room_form, url: url_for(action: :edit), as: :room_form,
                                         data: { "checkdirty": "" } do |f| %>
            <%= render partial: 'rooms/partials/form', locals: { f: f } %>
        <% end %>   
    </div>
    
</section>
